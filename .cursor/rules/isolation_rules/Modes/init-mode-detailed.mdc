---
alwaysApply: true
---

# GRAPH BANK INIT MODE

Your role is to perform comprehensive system initialization, health checks, and environment validation to ensure all components are ready before proceeding to subsequent modes.

> **TL;DR:** Provides unified system initialization, health checks, and environment validation processes to ensure all components are ready before starting subsequent modes.

```mermaid
graph TD
    Start["🚀 START INIT MODE"] --> ReadDocs["📚 Read Reference Documents<br>.cursor/rules/isolation_rules/main.mdc"]
    
    %% Initialization
    ReadDocs --> HealthCheck["🔍 System Health Check"]
    HealthCheck --> MCPCheck["🧠 MCP Graph Server<br>Status Check"]
    MCPCheck --> GraphCheck["📁 Graph Bank<br>Structure Check"]
    GraphCheck --> EnvCheck["⚙️ Environment<br>Validation"]
    EnvCheck --> ProjCheck["📂 Project Structure<br>Check"]
    
    %% Initialization Process
    ProjCheck --> InitMCP["🔧 Initialize MCP<br>Graph Server"]
    InitMCP --> InitGraph["📝 Initialize Graph Bank<br>Files"]
    InitGraph --> InitContext["🌐 Establish Base<br>Context"]
    InitContext --> StatusReport["📊 Generate Status<br>Report"]
    
    %% Verification
    StatusReport --> VerifyInit{"✅ System<br>Ready?"}
    VerifyInit -->|"Yes"| CompleteInit["🏁 INIT Process<br>Complete"]
    VerifyInit -->|"No"| FixIssues["🔧 Fix Identified<br>Issues"]
    FixIssues --> HealthCheck
    
    %% Mode Transitions
    CompleteInit -->|"Level 1"| TransToVan["→ VAN Mode"]
    CompleteInit -->|"Level 2-4"| TransToPlan["→ PLAN Mode"]
    CompleteInit -->|"Ready"| TransToImpl["→ IMPLEMENT Mode"]
    
    %% Validation Options
    Start -.-> Validation["🔍 VALIDATION OPTIONS:<br>- System health check<br>- Environment validation<br>- Component initialization<br>- Status reporting<br>- Mode transition"]
    
    %% Styling
    style Start fill:#4da6ff,stroke:#0066cc,color:white
    style ReadDocs fill:#80bfff,stroke:#4da6ff,color:black
    style HealthCheck fill:#ffa64d,stroke:#cc7a30,color:white
    style MCPCheck fill:#4dbb5f,stroke:#36873f,color:white
    style GraphCheck fill:#d94dbb,stroke:#a3378a,color:white
    style EnvCheck fill:#4dbbbb,stroke:#368787,color:white
    style ProjCheck fill:#d971ff,stroke:#a33bc2,color:white
    style VerifyInit fill:#d94dbb,stroke:#a3378a,color:white
    style CompleteInit fill:#5fd94d,stroke:#3da336,color:white
    style TransToVan fill:#ffa64d,stroke:#cc7a30,color:black
    style TransToPlan fill:#4dbb5f,stroke:#36873f,color:black
    style TransToImpl fill:#4da6ff,stroke:#0066cc,color:black
```

## IMPLEMENTATION STEPS

### Step 1: READ MAIN RULE & CONTEXT FILES
```
read_file({
  target_file: ".cursor/rules/isolation_rules/main.mdc",
  should_read_entire_file: true
})

read_file({
  target_file: "tasks.md",
  should_read_entire_file: true
})

read_file({
  target_file: "progress.md",
  should_read_entire_file: true
})
```

### Step 2: LOAD INIT MODE MAP
```
read_file({
  target_file: ".cursor/rules/isolation_rules/visual-maps/init-mode-map.mdc",
  should_read_entire_file: true
})
```

### Step 3: LOAD SYSTEM HEALTH CHECK REFERENCES
```
read_file({
  target_file: ".cursor/rules/isolation_rules/Core/system-health-check.mdc",
  should_read_entire_file: true
})

read_file({
  target_file: ".cursor/rules/isolation_rules/Core/environment-validation.mdc",
  should_read_entire_file: true
})
```

## INITIALIZATION APPROACH

Your task is to perform comprehensive system initialization, health checks, and environment validation. Focus on ensuring all components are ready before proceeding to subsequent modes.

### System Health Check Process

Perform thorough health checks on all system components to ensure they are functioning correctly and ready for development work.

```mermaid
graph TD
    SHC["🔍 SYSTEM HEALTH CHECK"] --> MCP["MCP Graph Server Check"]
    MCP --> Graph["Graph Bank Structure Check"]
    Graph --> Env["Environment Validation"]
    Env --> Proj["Project Structure Check"]
    Proj --> Report["Generate Health Report"]
    
    style SHC fill:#4da6ff,stroke:#0066cc,color:white
    style MCP fill:#4dbb5f,stroke:#36873f,color:white
    style Graph fill:#d94dbb,stroke:#a3378a,color:white
    style Env fill:#4dbbbb,stroke:#368787,color:white
    style Proj fill:#d971ff,stroke:#a33bc2,color:white
    style Report fill:#ffa64d,stroke:#cc7a30,color:white
```

### MCP Graph Server Initialization

Initialize and verify the MCP Graph Server connection, ensuring proper graph management and semantic search capabilities.

```mermaid
graph TD
    MCP["🧠 MCP GRAPH SERVER"] --> Check["Check Connection"]
    Check --> Validate["Validate Graph Structure"]
    Validate --> Test["Test Semantic Search"]
    Test --> Init["Initialize Graph Context"]
    Init --> Ready["Graph Server Ready"]
    
    style MCP fill:#4dbb5f,stroke:#36873f,color:white
    style Check fill:#d6f5dd,stroke:#a3e0ae,color:black
    style Validate fill:#d6f5dd,stroke:#a3e0ae,color:black
    style Test fill:#d6f5dd,stroke:#a3e0ae,color:black
    style Init fill:#d6f5dd,stroke:#a3e0ae,color:black
    style Ready fill:#d6f5dd,stroke:#a3e0ae,color:black
```

### Graph Bank Initialization

Initialize and verify the Graph Bank file structure, ensuring all necessary files are present and accessible.

```mermaid
graph TD
    GB["📁 GRAPH BANK INIT"] --> Check["Check Directory Structure"]
    Check --> Files["Verify Core Files"]
    Files --> Perms["Check File Permissions"]
    Perms --> Init["Initialize Missing Files"]
    Init --> Ready["Graph Bank Ready"]
    
    style GB fill:#d94dbb,stroke:#a3378a,color:white
    style Check fill:#f5d9f0,stroke:#e699d9,color:black
    style Files fill:#f5d9f0,stroke:#e699d9,color:black
    style Perms fill:#f5d9f0,stroke:#e699d9,color:black
    style Init fill:#f5d9f0,stroke:#e699d9,color:black
    style Ready fill:#f5d9f0,stroke:#e699d9,color:black
```

## INITIALIZATION DOCUMENTATION

Document each initialization step with clear progress indicators. Start by describing the system state, then perform health checks, initialize components, and conclude with a readiness report.

```mermaid
graph TD
    ID["🔧 INITIALIZATION DOCUMENTATION"] --> Start["🚀 START INITIALIZATION"]
    Start --> Health["System Health Assessment<br>What is the current system state?"]
    Health --> Check["Component Verification<br>Are all components accessible?"]
    Check --> Init["Component Initialization<br>Initialize missing or broken components"]
    Init --> Verify["System Verification<br>Does the system meet requirements?"] 
    Verify --> Report["Readiness Report<br>System ready for development"]
    Report --> Complete["🏁 INITIALIZATION COMPLETE"]
    
    style ID fill:#4da6ff,stroke:#0066cc,color:white
    style Start fill:#cce6ff,stroke:#80bfff,color:black
    style Health fill:#cce6ff,stroke:#80bfff,color:black
    style Check fill:#cce6ff,stroke:#80bfff,color:black
    style Init fill:#cce6ff,stroke:#80bfff,color:black
    style Verify fill:#cce6ff,stroke:#80bfff,color:black
    style Report fill:#cce6ff,stroke:#80bfff,color:black
    style Complete fill:#cce6ff,stroke:#80bfff,color:black
```

## VERIFICATION

```mermaid
graph TD
    V["✅ VERIFICATION CHECKLIST"] --> H["All health checks completed?"]
    V --> M["MCP Graph Server initialized?"]
    V --> G["Graph Bank structure verified?"]
    V --> E["Environment validation passed?"]
    V --> P["Project structure confirmed?"]
    V --> S["Status report generated?"]
    
    H & M & G & E & P & S --> Decision{"All Verified?"}
    Decision -->|"Yes"| Complete["Ready for next mode"]
    Decision -->|"No"| Fix["Complete missing items"]
    
    style V fill:#4dbbbb,stroke:#368787,color:white
    style Decision fill:#ffa64d,stroke:#cc7a30,color:white
    style Complete fill:#5fd94d,stroke:#3da336,color:white
    style Fix fill:#ff5555,stroke:#cc0000,color:white
```

Before completing the initialization phase, verify that all health checks have been completed, MCP Graph Server is initialized, Graph Bank structure is verified, environment validation has passed, project structure is confirmed, and status report has been generated. Update tasks.md with the initialization status and prepare for the next appropriate mode.
